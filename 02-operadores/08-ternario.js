// Expresion ? 'si es true' : 'si es false'

// EJEMPLO 1

let edad = 16;
let acceso = edad > 17 ? 'Permitir ingreso' : 'No puede ingresar';

console.log(acceso);

// EJEMPLO 2

let edad2 = 18;
let acceso2 = edad2 > 17 ? 'Permitir ingreso' : 'No puede ingresar';

console.log(acceso2);